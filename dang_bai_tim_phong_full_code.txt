--- File: src/app/pages/CreatePostPage.tsx ---
import { ArrowLeft, Check, Edit2 } from 'lucide-react';
import { useState } from 'react';
import { useNavigate } from 'react-router';

// Mock data - rooms available for posting
const availableRooms = [
  {
    id: 1,
    code: 'A-101',
    building: 'Tòa A',
    floor: 'Tầng 1',
    status: 'empty',
    area: 45,
    maxPeople: 4,
    price: '8.500.000',
    type: 'single',
    privateBathroom: true,
    amenities: ['Điều hòa', 'Nước nóng', 'Giường', 'Bàn làm việc'],
    services: [
      { name: 'Tiền điện', price: '3.500', unit: 'kWh' },
      { name: 'Tiền nước', price: '25.000', unit: 'm³' },
      { name: 'Phí quản lý', price: '500.000', unit: 'Tháng' },
      { name: 'Phí dọn rác', price: '50.000', unit: 'Tháng' },
    ],
    image: null,
  },
  {
    id: 2,
    code: 'A-108',
    building: 'Tòa A',
    floor: 'Tầng 1',
    status: 'empty',
    area: 50,
    maxPeople: 5,
    price: '9.000.000',
    type: 'apartment',
    rooms: { living: 1, bedroom: 2, kitchen: 1, bathroom: 2 },
    amenities: ['Điều hòa', 'Nước nóng', 'Tủ lạnh', 'Máy giặt', 'Giường'],
    services: [
      { name: 'Tiền điện', price: '3.500', unit: 'kWh' },
      { name: 'Tiền nước', price: '25.000', unit: 'm³' },
      { name: 'Phí quản lý', price: '500.000', unit: 'Tháng' },
      { name: 'Internet', price: '200.000', unit: 'Tháng' },
    ],
    image: null,
  },
  {
    id: 3,
    code: 'B-105',
    building: 'Tòa B',
    floor: 'Tầng 1',
    status: 'rented',
    contractEndDate: '15/08/2026',
    area: 42,
    maxPeople: 3,
    price: '7.800.000',
    type: 'single',
    privateBathroom: false,
    amenities: ['Điều hòa', 'Giường', 'Bàn làm việc'],
    services: [
      { name: 'Tiền điện', price: '3.500', unit: 'kWh' },
      { name: 'Tiền nước', price: '25.000', unit: 'm³' },
      { name: 'Phí quản lý', price: '500.000', unit: 'Tháng' },
    ],
    image: null,
  },
];

const statusLabels = {
  empty: 'Trống',
  rented: 'Đã thuê',
};

export function CreatePostPage() {
  const navigate = useNavigate();
  const [selectedRoomId, setSelectedRoomId] = useState<number | null>(null);
  const [moveInType, setMoveInType] = useState<'immediate' | 'from-date'>('immediate');
  const [floodProne, setFloodProne] = useState<'yes' | 'no'>('no');
  const [contactType, setContactType] = useState<'current' | 'other'>('current');
  const [editingServiceIndex, setEditingServiceIndex] = useState<number | null>(null);
  const [servicePrices, setServicePrices] = useState<{ [key: string]: string }>({});

  const selectedRoom = availableRooms.find(room => room.id === selectedRoomId);

  const handleServicePriceEdit = (index: number, currentPrice: string) => {
    setEditingServiceIndex(index);
    if (!servicePrices[`${selectedRoomId}-${index}`]) {
      setServicePrices({ ...servicePrices, [`${selectedRoomId}-${index}`]: currentPrice });
    }
  };

  const handleServicePriceChange = (index: number, value: string) => {
    setServicePrices({ ...servicePrices, [`${selectedRoomId}-${index}`]: value });
  };

  const handleServicePriceSave = () => {
    setEditingServiceIndex(null);
  };

  const getServicePrice = (index: number, defaultPrice: string) => {
    return servicePrices[`${selectedRoomId}-${index}`] || defaultPrice;
  };

  const handleSubmit = () => {
    // Handle form submission
    navigate('/post-management');
  };

  return (
    <div className="p-8 bg-gray-50 min-h-screen">
      {/* Header */}
      <div className="mb-6 flex items-center space-x-4">
        <button
          onClick={() => navigate('/post-management')}
          className="p-2 hover:bg-gray-200 rounded"
        >
          <ArrowLeft size={20} className="text-gray-600" />
        </button>
        <div>
          <h1 className="text-2xl font-bold text-gray-800 mb-1">Tạo bài đăng mới</h1>
          <p className="text-sm text-gray-600">Đăng tin tìm người thuê phòng</p>
        </div>
      </div>

      {/* Form */}
      <div className="bg-white border-2 border-gray-300 rounded max-w-5xl">
        <div className="p-6 space-y-6">
          {/* Section 1: Select Room */}
          <div className="space-y-4">
            <h4 className="text-base font-semibold text-gray-800 border-b pb-2">Chọn phòng muốn đăng</h4>

            <div className="grid grid-cols-1 gap-3">
              {availableRooms.map((room) => (
                <div
                  key={room.id}
                  onClick={() => setSelectedRoomId(room.id)}
                  className={`border-2 rounded p-4 cursor-pointer transition-all ${
                    selectedRoomId === room.id
                      ? 'border-blue-500 bg-blue-50'
                      : 'border-gray-300 hover:border-gray-400'
                  }`}
                >
                  <div className="flex items-start justify-between">
                    <div className="flex items-start space-x-3 flex-1">
                      <div className={`w-5 h-5 rounded border-2 flex items-center justify-center mt-0.5 ${
                        selectedRoomId === room.id
                          ? 'bg-blue-500 border-blue-500'
                          : 'border-gray-300'
                      }`}>
                        {selectedRoomId === room.id && <Check size={14} className="text-white" />}
                      </div>
                      <div className="flex-1">
                        <div className="flex items-center space-x-3 mb-1">
                          <span className="font-semibold text-gray-800">{room.code}</span>
                          <span className="text-sm text-gray-600">{room.building} - {room.floor}</span>
                        </div>
                        <div className="text-sm text-gray-700">
                          {room.status === 'empty' ? (
                            <span className="text-green-700 font-medium">Trống</span>
                          ) : (
                            <span className="text-red-700">Đã thuê tới ngày {room.contractEndDate}</span>
                          )}
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* Post Title (shown after room selection) */}
          {selectedRoom && (
            <>
              <div className="space-y-4">
                <h4 className="text-base font-semibold text-gray-800 border-b pb-2">Tiêu đề bài đăng</h4>
                <div>
                  <label className="block text-sm text-gray-700 mb-2">Nhập tiêu đề bài đăng *</label>
                  <input
                    type="text"
                    placeholder="Hãy tạo điểm nhấn để người thuê ấn tượng với phòng của bạn"
                    className="w-full px-4 py-3 text-sm border border-gray-300 rounded focus:outline-none focus:border-blue-500"
                  />
                  <p className="text-xs text-gray-500 mt-1">
                    VD: "Phòng đẹp thoáng mát giá rẻ gần trường ĐH", "Căn hộ 2PN full nội thất sang trọng"
                  </p>
                </div>
              </div>
            </>
          )}

          {/* Section 2: Room Information (shown after selection) */}
          {selectedRoom && (
            <>
              <div className="space-y-4">
                <h4 className="text-base font-semibold text-gray-800 border-b pb-2">Thông tin phòng</h4>

                <div className="bg-gray-50 border border-gray-300 rounded p-4">
                  <div className="grid grid-cols-2 gap-4">
                    <div>
                      <p className="text-sm text-gray-600 mb-1">Mã phòng</p>
                      <p className="text-sm font-semibold text-gray-800">{selectedRoom.code}</p>
                    </div>
                    <div>
                      <p className="text-sm text-gray-600 mb-1">Vị trí</p>
                      <p className="text-sm font-semibold text-gray-800">{selectedRoom.building} - {selectedRoom.floor}</p>
                    </div>
                    <div>
                      <p className="text-sm text-gray-600 mb-1">Diện tích</p>
                      <p className="text-sm font-semibold text-gray-800">{selectedRoom.area} m²</p>
                    </div>
                    <div>
                      <p className="text-sm text-gray-600 mb-1">Số người tối đa</p>
                      <p className="text-sm font-semibold text-gray-800">{selectedRoom.maxPeople} người</p>
                    </div>
                    <div>
                      <p className="text-sm text-gray-600 mb-1">Giá thuê</p>
                      <p className="text-sm font-semibold text-gray-800">{selectedRoom.price} VNĐ/tháng</p>
                    </div>
                    <div>
                      <p className="text-sm text-gray-600 mb-1">Loại phòng</p>
                      <p className="text-sm font-semibold text-gray-800">
                        {selectedRoom.type === 'single' ? 'Phòng đơn' : 'Căn hộ'}
                      </p>
                    </div>
                  </div>

                  {/* Room-specific details */}
                  {selectedRoom.type === 'single' ? (
                    <div className="mt-4 pt-4 border-t border-gray-300">
                      <p className="text-sm text-gray-600">
                        {selectedRoom.privateBathroom ? '✓ Có vệ sinh khép kín' : '✗ Không có vệ sinh khép kín'}
                      </p>
                    </div>
                  ) : (
                    <div className="mt-4 pt-4 border-t border-gray-300">
                      <p className="text-sm text-gray-600 mb-2">Cấu trúc căn hộ:</p>
                      <div className="grid grid-cols-4 gap-2 text-sm text-gray-700">
                        <div>{selectedRoom.rooms?.living} phòng khách</div>
                        <div>{selectedRoom.rooms?.bedroom} phòng ngủ</div>
                        <div>{selectedRoom.rooms?.kitchen} phòng bếp</div>
                        <div>{selectedRoom.rooms?.bathroom} phòng vệ sinh</div>
                      </div>
                    </div>
                  )}

                  {/* Amenities */}
                  <div className="mt-4 pt-4 border-t border-gray-300">
                    <p className="text-sm text-gray-600 mb-2">Tiện nghi:</p>
                    <div className="flex flex-wrap gap-2">
                      {selectedRoom.amenities.map((amenity, idx) => (
                        <span key={idx} className="px-2 py-1 bg-white border border-gray-300 rounded text-xs text-gray-700">
                          {amenity}
                        </span>
                      ))}
                    </div>
                  </div>

                  {/* Services */}
                  <div className="mt-4 pt-4 border-t border-gray-300">
                    <p className="text-sm text-gray-600 mb-3">Dịch vụ đi kèm:</p>
                    <div className="border border-gray-300 rounded overflow-hidden">
                      <table className="w-full">
                        <thead className="bg-gray-50">
                          <tr>
                            <th className="px-4 py-2 text-left text-xs font-semibold text-gray-600 border-b border-gray-300">
                              Tên dịch vụ
                            </th>
                            <th className="px-4 py-2 text-right text-xs font-semibold text-gray-600 border-b border-gray-300">
                              Giá tiền
                            </th>
                          </tr>
                        </thead>
                        <tbody>
                          {selectedRoom.services.map((service: any, idx: number) => (
                            <tr key={idx} className="border-b border-gray-200 last:border-b-0 hover:bg-gray-50">
                              <td className="px-4 py-3 text-sm text-gray-700">{service.name}</td>
                              <td className="px-4 py-3 text-right">
                                <div className="flex items-center justify-end space-x-2">
                                  {editingServiceIndex === idx ? (
                                    <>
                                      <input
                                        type="text"
                                        value={getServicePrice(idx, service.price)}
                                        onChange={(e) => handleServicePriceChange(idx, e.target.value)}
                                        className="w-32 px-2 py-1 text-sm text-right border border-gray-300 rounded focus:outline-none focus:border-blue-500"
                                        autoFocus
                                      />
                                      <span className="text-xs text-gray-600">VNĐ/{service.unit}</span>
                                      <button
                                        onClick={handleServicePriceSave}
                                        className="px-2 py-1 text-xs bg-blue-500 text-white rounded hover:bg-blue-600"
                                      >
                                        Lưu
                                      </button>
                                    </>
                                  ) : (
                                    <>
                                      <span className="text-sm text-gray-800 font-medium">
                                        {getServicePrice(idx, service.price)}
                                      </span>
                                      <span className="text-xs text-gray-600">VNĐ/{service.unit}</span>
                                      <button
                                        onClick={() => handleServicePriceEdit(idx, service.price)}
                                        className="p-1 hover:bg-gray-200 rounded"
                                        title="Chỉnh sửa giá"
                                      >
                                        <Edit2 size={14} className="text-gray-600" />
                                      </button>
                                    </>
                                  )}
                                </div>
                              </td>
                            </tr>
                          ))}
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
              </div>

              {/* Section 3: Move-in Availability */}
              <div className="space-y-4">
                <h4 className="text-base font-semibold text-gray-800 border-b pb-2">Thời gian vào ở</h4>

                <div>
                  <label className="block text-sm text-gray-700 mb-3">Có thể vào ở *</label>
                  <div className="space-y-3">
                    <div className="flex items-center space-x-2">
                      <input
                        type="radio"
                        id="movein-immediate"
                        name="movein"
                        value="immediate"
                        checked={moveInType === 'immediate'}
                        onChange={(e) => setMoveInType(e.target.value as 'immediate' | 'from-date')}
                        className="w-4 h-4"
                      />
                      <label htmlFor="movein-immediate" className="text-sm text-gray-700">Ở luôn</label>
                    </div>
                    <div className="flex items-center space-x-2">
                      <input
                        type="radio"
                        id="movein-fromdate"
                        name="movein"
                        value="from-date"
                        checked={moveInType === 'from-date'}
                        onChange={(e) => setMoveInType(e.target.value as 'immediate' | 'from-date')}
                        className="w-4 h-4"
                      />
                      <label htmlFor="movein-fromdate" className="text-sm text-gray-700">Từ</label>
                      {moveInType === 'from-date' && (
                        <input
                          type="date"
                          className="px-3 py-1 text-sm border border-gray-300 rounded focus:outline-none focus:border-gray-500"
                        />
                      )}
                    </div>
                  </div>
                </div>
              </div>

              {/* Section 4: Additional Information */}
              <div className="space-y-4">
                <h4 className="text-base font-semibold text-gray-800 border-b pb-2">Thông tin bổ sung</h4>

                <div>
                  <label className="block text-sm text-gray-700 mb-2">Có nằm trong khu vực dễ ngập lụt *</label>
                  <div className="flex items-center space-x-6">
                    <div className="flex items-center space-x-2">
                      <input
                        type="radio"
                        id="flood-yes"
                        name="flood"
                        value="yes"
                        checked={floodProne === 'yes'}
                        onChange={(e) => setFloodProne(e.target.value as 'yes' | 'no')}
                        className="w-4 h-4"
                      />
                      <label htmlFor="flood-yes" className="text-sm text-gray-700">Có</label>
                    </div>
                    <div className="flex items-center space-x-2">
                      <input
                        type="radio"
                        id="flood-no"
                        name="flood"
                        value="no"
                        checked={floodProne === 'no'}
                        onChange={(e) => setFloodProne(e.target.value as 'yes' | 'no')}
                        className="w-4 h-4"
                      />
                      <label htmlFor="flood-no" className="text-sm text-gray-700">Không</label>
                    </div>
                  </div>
                </div>

                <div>
                  <label className="block text-sm text-gray-700 mb-2">Yêu cầu từ chủ nhà khi cho thuê (nếu có)</label>
                  <textarea
                    rows={3}
                    placeholder="VD: Không nuôi thú cưng, không hút thuốc trong phòng..."
                    className="w-full px-3 py-2 text-sm border border-gray-300 rounded focus:outline-none focus:border-gray-500"
                  />
                </div>
              </div>

              {/* Section 5: Contact Information */}
              <div className="space-y-4">
                <h4 className="text-base font-semibold text-gray-800 border-b pb-2">Thông tin liên hệ</h4>

                <div>
                  <label className="block text-sm text-gray-700 mb-3">Chọn thông tin liên hệ *</label>
                  <div className="space-y-3">
                    <div className="flex items-center space-x-2">
                      <input
                        type="radio"
                        id="contact-current"
                        name="contact"
                        value="current"
                        checked={contactType === 'current'}
                        onChange={(e) => setContactType(e.target.value as 'current' | 'other')}
                        className="w-4 h-4"
                      />
                      <label htmlFor="contact-current" className="text-sm text-gray-700">
                        Lấy từ tài khoản đang dùng <span className="text-gray-500">(Nguyễn Văn A - 0912345678)</span>
                      </label>
                    </div>
                    <div className="flex items-center space-x-2">
                      <input
                        type="radio"
                        id="contact-other"
                        name="contact"
                        value="other"
                        checked={contactType === 'other'}
                        onChange={(e) => setContactType(e.target.value as 'current' | 'other')}
                        className="w-4 h-4"
                      />
                      <label htmlFor="contact-other" className="text-sm text-gray-700">Khác (Nhập thủ công)</label>
                    </div>
                  </div>

                  {contactType === 'other' && (
                    <div className="mt-4 grid grid-cols-2 gap-4 pl-6 border-l-2 border-gray-300">
                      <div>
                        <label className="block text-sm text-gray-700 mb-2">Tên người liên hệ *</label>
                        <input
                          type="text"
                          placeholder="VD: Nguyễn Văn B"
                          className="w-full px-3 py-2 text-sm border border-gray-300 rounded focus:outline-none focus:border-gray-500"
                        />
                      </div>
                      <div>
                        <label className="block text-sm text-gray-700 mb-2">Số điện thoại *</label>
                        <input
                          type="tel"
                          placeholder="VD: 0987654321"
                          className="w-full px-3 py-2 text-sm border border-gray-300 rounded focus:outline-none focus:border-gray-500"
                        />
                      </div>
                    </div>
                  )}
                </div>
              </div>
            </>
          )}
        </div>

        {/* Footer Actions */}
        <div className="border-t border-gray-300 px-6 py-4 flex items-center justify-end space-x-3">
          <button
            onClick={() => navigate('/post-management')}
            className="px-4 py-2 bg-white border border-gray-300 text-gray-700 text-sm rounded hover:bg-gray-50"
          >
            Hủy
          </button>
          <button
            onClick={handleSubmit}
            disabled={!selectedRoomId}
            className={`px-4 py-2 text-white text-sm rounded ${
              selectedRoomId
                ? 'bg-gray-800 hover:bg-gray-700'
                : 'bg-gray-400 cursor-not-allowed'
            }`}
          >
            Đăng bài
          </button>
        </div>
      </div>
    </div>
  );
}

--- File: src/app/pages/PostManagementPage.tsx ---
import { Plus, Eye, MessageCircle, Edit2, Lock, Unlock, X, History, AlertTriangle, Upload, Image as ImageIcon } from 'lucide-react';
import { useState } from 'react';
import { useNavigate } from 'react-router';

const postsData = [
  {
    id: 1,
    room: 'A-101',
    building: 'Tòa A',
    postDate: '15/04/2026',
    views: 234,
    messages: 12,
    status: 'empty',
    isLocked: false,
    title: 'Phòng đẹp thoáng mát giá rẻ gần trường ĐH',
    area: 45,
    price: '8.500.000',
    maxPeople: 4,
    type: 'Phòng đơn',
    amenities: ['Điều hòa', 'Nước nóng', 'Giường', 'Bàn làm việc'],
  },
  {
    id: 2,
    room: 'A-203',
    building: 'Tòa A',
    postDate: '10/04/2026',
    views: 189,
    messages: 8,
    status: 'rented',
    isLocked: true,
    title: 'Căn hộ 2PN full nội thất sang trọng',
    area: 50,
    price: '9.000.000',
    maxPeople: 5,
    type: 'Căn hộ',
    amenities: ['Điều hòa', 'Tủ lạnh', 'Máy giặt'],
  },
  {
    id: 3,
    room: 'B-105',
    building: 'Tòa B',
    postDate: '08/04/2026',
    views: 312,
    messages: 15,
    status: 'empty',
    isLocked: false,
    title: 'Phòng trọ giá sinh viên gần siêu thị',
    area: 42,
    price: '7.800.000',
    maxPeople: 3,
    type: 'Phòng đơn',
    amenities: ['Điều hòa', 'Giường'],
  },
  {
    id: 4,
    room: 'A-108',
    building: 'Tòa A',
    postDate: '05/04/2026',
    views: 156,
    messages: 5,
    status: 'empty',
    isLocked: false,
    title: 'Phòng mới xây full nội thất',
    area: 48,
    price: '8.800.000',
    maxPeople: 4,
    type: 'Phòng đơn',
    amenities: ['Điều hòa', 'Nước nóng'],
  },
  {
    id: 5,
    room: 'B-202',
    building: 'Tòa B',
    postDate: '01/04/2026',
    views: 278,
    messages: 10,
    status: 'rented',
    isLocked: false,
    title: 'Chung cư mini cao cấp view đẹp',
    area: 55,
    price: '10.000.000',
    maxPeople: 6,
    type: 'Căn hộ',
    amenities: ['Điều hòa', 'Tủ lạnh', 'Máy giặt', 'Ban công'],
  },
];

const statusConfig = {
  empty: { label: 'Trống', bgColor: '#D1FAE5', textColor: '#065F46', borderColor: '#A7F3D0' },
  rented: { label: 'Đã thuê', bgColor: '#FEE2E2', textColor: '#991B1B', borderColor: '#FECACA' },
};

// Mock edit history with full versions
const editHistory: any = {
  1: [
    {
      version: 1,
      date: '15/04/2026 14:30',
      user: 'Nguyễn Văn A',
      action: 'Tạo bài đăng',
      data: {
        title: 'Phòng đẹp thoáng mát giá rẻ gần trường ĐH',
        room: 'A-101',
        building: 'Tòa A',
        area: 45,
        maxPeople: 4,
        price: '8.500.000',
        type: 'single',
        privateBathroom: true,
        amenities: ['Điều hòa', 'Nước nóng', 'Giường', 'Bàn làm việc'],
        services: [
          { name: 'Tiền điện', price: '3.500', unit: 'kWh' },
          { name: 'Tiền nước', price: '25.000', unit: 'm³' },
          { name: 'Phí quản lý', price: '500.000', unit: 'Tháng' },
          { name: 'Phí dọn rác', price: '50.000', unit: 'Tháng' },
        ],
        moveInType: 'immediate',
        floodProne: 'no',
        landlordRequirements: 'Không nuôi thú cưng, không hút thuốc trong phòng',
        contactType: 'current',
      },
    },
  ],
  2: [
    {
      version: 2,
      date: '12/04/2026 10:20',
      user: 'Nguyễn Văn A',
      action: 'Cập nhật giá phòng',
      data: {
        title: 'Căn hộ 2PN full nội thất sang trọng',
        room: 'A-203',
        price: '9.000.000',
      },
    },
    {
      version: 1,
      date: '10/04/2026 09:15',
      user: 'Nguyễn Văn A',
      action: 'Tạo bài đăng',
      data: {
        title: 'Căn hộ 2PN full nội thất sang trọng',
        room: 'A-203',
        price: '8.500.000',
      },
    },
  ],
};

export function PostManagementPage() {
  const navigate = useNavigate();
  const [posts, setPosts] = useState(postsData);
  const [selectedPost, setSelectedPost] = useState<any>(null);
  const [showEditModal, setShowEditModal] = useState(false);
  const [showHistoryModal, setShowHistoryModal] = useState(false);
  const [showVersionModal, setShowVersionModal] = useState(false);
  const [selectedVersion, setSelectedVersion] = useState<any>(null);
  const [showConfirmLockModal, setShowConfirmLockModal] = useState(false);
  const [pendingLockPost, setPendingLockPost] = useState<any>(null);
  const [imagePreview, setImagePreview] = useState<string[]>([]);
  const [moveInType, setMoveInType] = useState<'immediate' | 'from-date'>('immediate');
  const [contactType, setContactType] = useState<'current' | 'other'>('current');

  const handleCreatePost = () => {
    navigate('/post-management/create');
  };

  const handleViewMessages = (roomCode: string) => {
    navigate(`/messages?room=${roomCode}`);
  };

  const handleEditPost = (post: any) => {
    setSelectedPost(post);
    setShowEditModal(true);
  };

  const handleShowHistory = () => {
    setShowHistoryModal(true);
  };

  const handleConfirmLock = (post: any) => {
    setPendingLockPost(post);
    setShowConfirmLockModal(true);
  };

  const handleToggleLock = () => {
    if (pendingLockPost) {
      setPosts(posts.map(post =>
        post.id === pendingLockPost.id ? { ...post, isLocked: !post.isLocked } : post
      ));
      setShowConfirmLockModal(false);
      setPendingLockPost(null);
    }
  };

  const handleSaveEdit = () => {
    // Save logic here
    setShowEditModal(false);
  };

  const handleViewVersion = (version: any) => {
    setSelectedVersion(version);
    setShowVersionModal(true);
  };

  return (
    <div className="p-8 bg-gray-50 min-h-screen">
      {/* Header */}
      <div className="mb-6">
        <h1 className="text-2xl font-bold text-gray-800 mb-2">Đăng bài tìm phòng</h1>
        <p className="text-sm text-gray-600">Quản lý các bài đăng tìm người thuê phòng</p>
      </div>

--- File: src/app/routes.ts ---
import { createBrowserRouter } from "react-router";
import { LandingPage } from "./pages/LandingPage";
import { PlatformSelector } from "./components/PlatformSelector";
import { AppSelector } from "./components/AppSelector";
import { AdminLayout } from "./components/AdminLayout";
import { WireframeOverviewPage } from "./pages/WireframeOverviewPage";
import { DashboardPage } from "./pages/DashboardPage";
import { BuildingManagementPage } from "./pages/BuildingManagementPage";
import { ServicePricingPage } from "./pages/ServicePricingPage";
import { AssetInventoryPage } from "./pages/AssetInventoryPage";
import { ResidentManagementPage } from "./pages/ResidentManagementPage";
import { ContractManagementPage } from "./pages/ContractManagementPage";
import { SettlementPage } from "./pages/SettlementPage";
import { UtilityReadingPage } from "./pages/UtilityReadingPage";
import { InvoiceManagementPage } from "./pages/InvoiceManagementPage";
import { TransactionHistoryPage } from "./pages/TransactionHistoryPage";
import { DebtManagementPage } from "./pages/DebtManagementPage";
import { MaintenanceRequestPage } from "./pages/MaintenanceRequestPage";
import { KnowledgeBasePage } from "./pages/KnowledgeBasePage";
import { ChatHistoryPage } from "./pages/ChatHistoryPage";
import { RevenueReportPage } from "./pages/RevenueReportPage";
import { OccupancyReportPage } from "./pages/OccupancyReportPage";
import { DebtReportPage } from "./pages/DebtReportPage";
import { UserAccountsPage } from "./pages/UserAccountsPage";
import { MyProfilePage } from "./pages/MyProfilePage";
import { AuditLogsPage } from "./pages/AuditLogsPage";
import { ResidentLayout } from "./components/resident/ResidentLayout";
import { ResidentHome } from "./components/resident/ResidentHome";
import { BillDetail } from "./components/resident/BillDetail";
import { PaymentQR } from "./components/resident/PaymentQR";
import { CreateIncident } from "./components/resident/CreateIncident";
import { IncidentTracking } from "./components/resident/IncidentTracking";
import { IncidentList } from "./components/resident/IncidentList";
import { ChatAI } from "./components/resident/ChatAI";
import { ResidentProfile } from "./components/resident/ResidentProfile";
import { PaymentHistory } from "./components/resident/PaymentHistory";
import { NotificationList } from "./components/resident/NotificationList";
import { PostManagementPage } from "./pages/PostManagementPage";
import { CreatePostPage } from "./pages/CreatePostPage";
import { MessagesPage } from "./pages/MessagesPage";

export const router = createBrowserRouter([
  {
    path: "/",
    Component: LandingPage,
  },
  {
    path: "/select-platform",
    Component: PlatformSelector,
  },
  {
    path: "/select-app",
    Component: AppSelector,
  },
  // Admin Routes with Layout
  {
    path: "/",
    Component: AdminLayout,
    children: [
      {
        path: "dashboard",
        Component: DashboardPage,
      },
@@
      {
        path: "post-management",
        Component: PostManagementPage,
      },
      {
        path: "post-management/create",
        Component: CreatePostPage,
      },
@@
    ],
  },
  // Resident App Routes
  {
    path: "/resident",
    Component: ResidentLayout,
    children: [
      {
        index: true,
        Component: ResidentHome,
      },
@@
    ],
  },
]);
